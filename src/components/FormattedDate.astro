---
import { LOCALE } from "../config";
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="date-time">
  {date.toLocaleDateString(LOCALE.langTag)}
</time>
<style>
  .date-time {
    @apply text-sm italic opacity-80;
  }
</style>
